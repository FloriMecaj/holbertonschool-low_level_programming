#include "main.h"

/**
 * add - Entry point
 * @first_n: -first number
 * @second_n: second num
 * Return the sum of two int numbers;
 * Return: 0
 */

int add(int a, int b)
{
	int add;

	add = a + b;
	return (add);
}
